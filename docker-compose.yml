services:
  db:
    image: postgres:17.0
    hostname: localhost
    ports:
      - 5432:5432
    environment:
      - POSTGRES_PASSWORD=password
      - POSTGRES_USER=postgres
      - POSTGRES_DB=dirzzle-orm
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
